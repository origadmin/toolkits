syntax = "proto3";

package middleware.logger.v1;

option go_package = "github.com/origadmin/toolkits/runtime/middlewares";

//import "google/protobuf/duration.proto";

// Logger middleware config.
message LoggerConfig {
  bool enabled = 1;

  string name = 2;
}