/*
 * Copyright (c) 2024 OrigAdmin. All rights reserved.
 */

// Package fileupload implements the functions, types, and interfaces for the module.
package fileupload

import (
	"testing"
)

func TestUpDown(t *testing.T) {

	//// HTTP 上传
	//uploader, _ := builder.NewUploader(ctx)
	//uploader.SetFileHeader(ctx, header)
	//uploader.UploadFile(ctx, reader)
	//resp, _ := uploader.Finalize(ctx)
	//
	//// gRPC 下载
	//downloader, _ := builder.NewDownloader(ctx)
	//header, _ := downloader.GetFileHeader(ctx)
	//reader, _ := downloader.DownloadFile(ctx)
	//downloader.Finalize(ctx, resp)
}
